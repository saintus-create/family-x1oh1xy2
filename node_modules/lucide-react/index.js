const React = require("react");

function makeIcon(inner) {
  return function Icon(props) {
    const {
      size = 24,
      color,
      strokeWidth = 2,
      absoluteStrokeWidth,
      strokeLinecap,
      strokeLinejoin,
      style,
      className,
      ...rest
    } = props || {};
    return React.createElement(
      "svg",
      {
        xmlns: "http://www.w3.org/2000/svg",
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color || "currentColor",
        strokeWidth: strokeWidth,
        strokeLinecap: strokeLinecap || "round",
        strokeLinejoin: strokeLinejoin || "round",
        className: (className ? className + " " : "") + "lucide",
        style: style,
        ...rest,
      },
      inner
    );
  };
}

const Circle = makeIcon(React.createElement("circle", { cx: 12, cy: 12, r: 10 }));

module.exports = { Circle };
module.exports.default = { Circle };
